"use client";
import {useEffect,useState} from 'react';
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';
export default function Appearance(){const [theme,setTheme]=useState('midnight');useEffect(()=>{try{const saved=localStorage.getItem('beacon-appearance');if(['midnight','graphite','deep-blue'].includes(saved||'')){setTheme(saved!);document.documentElement.dataset.appearance=saved!;}}catch{}document.documentElement.classList.add('dark');},[]);function change(value:string){setTheme(value);document.documentElement.dataset.appearance=value;try{localStorage.setItem('beacon-appearance',value);}catch{}}return <Select value={theme} onValueChange={change}><SelectTrigger aria-label="Blue and black appearance" className="appearance-picker"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="midnight">Midnight</SelectItem><SelectItem value="graphite">Graphite</SelectItem><SelectItem value="deep-blue">Deep Blue</SelectItem></SelectContent></Select>;}
